app.controller('controllerHome', ['$scope', '$http', '$window',
    function($scope, $http, $window) {

        init();

        function init() {
            console.log('ci sono!');
        }

    }
]);